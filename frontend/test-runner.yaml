name: Test Self-Hosted Runner

on:
  push:
    branches:
      - main

jobs:
  test-runner:
    runs-on: self-hosted
    steps:
      - name: Check running machine
        run: |
          echo "Hello from self-hosted runner!"
          echo "Hostname: $(hostname)"
          whoami
